language: cpp

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - george-edison55-precise-backports
    packages:
    - cmake
    - cmake-data
    - gcc-6
    - g++-6
    - libx11-dev
    - libxrandr-dev
    - libxinerama-dev
    - libxxf86vm-dev
    - libxcursor-dev
    - x11proto-kb-dev
    - libsdl2-dev

compiler:
  - gcc
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
